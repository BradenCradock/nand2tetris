import sys
import parse

if __name__ == "__main__":
    #asmFilePath = str(sys.argv[1])

    f = open("Prog.asm","w+")      #Uses current directory
    while parse.hasMoreCommands():
        parse.advance()
        if parse.commandType() in {"C_PUSH", "C_POP", "C_FUNCTION", "C_CALL"}:
            print(parse.arg2())
    f.close
